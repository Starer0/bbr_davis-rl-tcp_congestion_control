# BBR-Davis RL 训练与实验对话记录

## 2026-05-22

### 训练恢复与奖励修正
- 从 step 12352 resume 继续训练 PPO，目标 50000 步
- 发现低频宽下 sum_reward 超过理论上限（134 > 100）
- 原因：iperf3 测量精度不足 + ss RTT 采样偏低
- 修复：在 `mahimahi_env.py` 中给 reward 加 `np.clip(reward, 0.0, 1.0)`
- ep_rew_mean 变化记录（从 step 0 开始，所有修复已应用）：
  - 第一轮（0→12352）：5.61 → 14 → 10.8 → 18.2 → 23.7 → 20.1 → 16.5 → 34.3 → 32.2 → 30.5 → 32.9 → 32.2 → 31.6 → 29
  - 第二轮（12352→20193）：12.6 → 9.06 → 6.78 → 9.4 → 9.79 → 12.8 → 14.9 → 17.4 → 18.4 → 18.9 → 20.4 → 19.7 → 19.2 → 19.9

### 训练收敛判断
- 20193 步时 ep_rew_mean 稳定在 18-20，判定收敛
- 训练过程中存在 reward clip 修复未生效的问题（代码改动需重启）

### 实验工具配置
- 用户询问实验运行方式，查看了 README.md 中的实验说明
- 需要使用 `./run_rl_experiment.sh` + trace 参数

### RL 日志功能添加
- 用户要求添加实验日志，记录每次 target 调整的参数
- 修改 `rl_controller.py`：
  - 添加 `--trace-name` 和 `--log-dir` 参数
  - 实验启动时生成 CSV 日志文件（命名格式：`{trace_name}_{YYYY-MM-DD_HH-MM-SS}.csv`）
  - 每步记录：step, time_s, tp_bps, tp_mbps, rtt_us, rtt_ms, retransmits, bytes_sent, retrans_rate, old_target_us, delta, new_target_us, obs_tp, obs_rtt, obs_retrans, obs_target
- 修改 `run_rl_experiment.sh`：
  - 自动创建 logs 目录
  - 传递 trace-name 和 log-dir 给 rl_controller.py

## 2026-05-23

### 模型退化问题发现
- 用户用 `run_rl_experiment.sh` 跑实验，发现 RL 效果很差
- 检查日志发现模型在所有条件下始终输出 `delta=1.0`，target 瞬间到 150000 并焊死
- 两个 trace（TMobile-UMTS, Verizon-EVDO）结果完全一致：751 步全部 delta=1.0
- 诊断：模型学到了退化策略——"无论如何把 target 往最大推"
- 原因分析：target=150000 时 C2TCP 接近原生 BBR，在大多数训练条件下不差，模型没有学到降低 target 的动机

### 数据对比与控制实验
- 用户对比了两个版本的性能（同 trace：TMobile-UMTS-driving）：

| 版本 | 利用率 | 单包时延 |
|------|--------|----------|
| 旧版 bbr_davis（无 RL），target=150ms 固定 | 90.1% | 63ms |
| 新版 bbr_davis，target=150ms 固定 | 82.1% | 69ms |
| 新版 bbr_davis + RL | 46.5% | 63ms |

- 排除变量：
  - `-i 1` vs `-i 0`：mm-tcp 改 `-i 1` 后仍然 94% 利用率，确认非原因
  - 真正问题：旧版和新版的模块代码不同

### 旧版 vs 新版内核模块差异
- diff 分析关键差异：
  1. **C2TCP detector 调用顺序**：旧版在 `bbr_set_cwnd` 之后，新版在之前
  2. **新版注释**：旧的自动探索逻辑被注释，改为 "RL-controlled via module param"
  3. **target 来源**：旧版硬编码 150000，新版读 module param

### RL Controller 修复
- 问题：RL controller 每秒写 sysfs（即使值不变），扰动内核 tuner 状态
- 修复 `rl_controller.py`：
  - 添加 `WRITE_THRESHOLD = 0.01`（变化超过 1% 才写 sysfs）
  - 计算 new_target 后先 clip，只有真实变化时才写入

### 待验证
- RL controller 修复后的实验效果未跑
- 需要重跑的对照实验（同 trace TMobile-UMTS-driving）：
  1. `mm-tcp` + 固定 target=150000
  2. `mm-tcp` + 固定 target=100000
  3. `run_rl_experiment.sh` + RL
